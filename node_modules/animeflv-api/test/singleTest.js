const { searchAnime, searchAnimesByFilter, getLatest } = require("../dist")

searchAnime("bocchi")
    .then(animes => {
        console.log(animes.data.length);
    })

searchAnimesByFilter({
    genres: ["Acción", "Ecchi"],
    order: "Nombre A-Z"
}).then(animes => {
    console.log(animes.data.length);
})

getLatest().then(latest => {
    console.log(latest.length);
})